/** 
1. Create an arrow function called square that takes a number as an agument and retuns its square. Use the arrow function to calculate the square of a given number and display the result.
*/
const square = (num) => {
    return Math.pow(num, 2)
}

console.log(square(4))